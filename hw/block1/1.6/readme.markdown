# Task 1.6

## Description

Collect all deadlock samples from the first lecture slides and make them runnable Java programs.
Run every sample, ensure it is deadlocked by using [jstack](https://docs.oracle.com/javase/8/docs/technotes/tools/unix/jstack.html).

## Requirements

Provide source code for every sample, provide `jstack` output for every deadlocked application, describe in few sentences why obtained stacktrace-dump confirms that your application indeed hang in deadlock.
